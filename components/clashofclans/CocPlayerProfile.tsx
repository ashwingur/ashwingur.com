import React from "react";
import { Player } from "../../shared/interfaces/coc.interface";

interface CocPlayerProfileProps {
  player: Player;
}

const CocPlayerProfile = ({ player }: CocPlayerProfileProps) => {
  return <div>CocPlayerProfile</div>;
};

export default CocPlayerProfile;
